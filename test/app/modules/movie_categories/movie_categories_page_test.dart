import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:ma9filmes/app/modules/movie_categories/movie_categories_page.dart';

main() {
  testWidgets('MovieCategoriesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MovieCategoriesPage(title: 'MovieCategories')));
    //  final titleFinder = find.text('MovieCategories');
    //  expect(titleFinder, findsOneWidget);
  });
}
