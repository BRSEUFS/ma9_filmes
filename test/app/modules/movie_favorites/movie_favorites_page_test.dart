import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:ma9filmes/app/modules/movie_favorites/movie_favorites_page.dart';

main() {
  testWidgets('MovieFavoritesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MovieFavoritesPage(title: 'MovieFavorites')));
    //  final titleFinder = find.text('MovieFavorites');
    //  expect(titleFinder, findsOneWidget);
  });
}
