import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:ma9filmes/app/modules/movie_detail/movie_detail_page.dart';

main() {
  testWidgets('MovieDetailPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MovieDetailPage(title: 'MovieDetail')));
    //  final titleFinder = find.text('MovieDetail');
    //  expect(titleFinder, findsOneWidget);
  });
}
